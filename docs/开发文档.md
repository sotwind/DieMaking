# 刀模管理系统 - 开发文档

## 1. 项目概述

刀模管理系统是一款基于 .NET Windows Forms 的桌面应用程序，用于管理纸箱刀模的全生命周期，包括刀模信息、生产工序、仓库库存、借用归还等业务流程。

## 2. 项目架构

### 2.1 目录结构

```
DieMaking/
├── App.config              # 应用程序配置文件
├── DieMaking.csproj        # 项目文件
├── Program.cs              # 程序入口
├── README.md               # 项目说明
├── docs/                   # 文档目录
│   ├── 开发文档.md
│   ├── 部署文档.md
│   └── 用户操作手册.md
├── Forms/                  # 窗体界面
│   ├── Common/             # 通用窗体
│   │   ├── BaseForm.cs
│   │   ├── ErrorDialog.cs
│   │   └── ImportPreviewForm.cs
│   ├── Die/                # 刀模管理
│   │   ├── DieListForm.cs
│   │   └── DieAddForm.cs
│   ├── Production/         # 生产管理
│   │   ├── CompletionQueryForm.cs
│   │   ├── ProcessReportForm.cs
│   │   └── ProductionBoardForm.cs
│   ├── Report/             # 报表统计
│   │   ├── CompletionStatsForm.cs
│   │   ├── DeliveryWarningForm.cs
│   │   ├── EmployeePerformanceForm.cs
│   │   ├── InventoryStatsForm.cs
│   │   └── ProcessStatsForm.cs
│   ├── System/             # 系统管理
│   │   ├── BackupManageForm.cs
│   │   ├── OperationLogForm.cs
│   │   ├── SettingsForm.cs
│   │   ├── UserManageForm.cs
│   │   └── UserSettingsForm.cs
│   ├── Warehouse/          # 仓库管理
│   │   ├── BorrowRecordForm.cs
│   │   ├── DieBorrowForm.cs
│   │   ├── DieReturnForm.cs
│   │   ├── DieStorageForm.cs
│   │   ├── LocationManageForm.cs
│   │   └── ScrapApplyForm.cs
│   ├── LoginForm.cs        # 登录窗体
│   └── MainForm.cs         # 主窗体
├── Helpers/                # 辅助类
│   ├── ConfigHelper.cs     # 配置管理
│   ├── DatabaseHealthChecker.cs
│   ├── DatabaseInitializer.cs
│   ├── DatabaseMigration.cs
│   ├── DataGridViewHelper.cs
│   ├── DbHelper.cs         # 数据库访问
│   ├── ExceptionHelper.cs  # 异常处理
│   ├── ShortcutHelper.cs   # 快捷键处理
│   ├── SqlPerformanceMonitor.cs
│   └── UIStyleHelper.cs    # UI样式
├── Models/                 # 数据模型
│   ├── DieInfo.cs          # 刀模相关模型
│   ├── OperationLog.cs     # 操作日志
│   ├── StorageLocation.cs  # 仓库相关模型
│   ├── SystemConfig.cs     # 系统配置
│   └── User.cs             # 用户模型
└── Services/               # 业务服务
    ├── BackupService.cs
    ├── ConfigService.cs
    ├── DieService.cs       # 刀模服务
    ├── ImportExportService.cs
    ├── LogService.cs
    ├── PrintService.cs
    ├── ProductionService.cs # 生产服务
    ├── ReportService.cs    # 报表服务
    ├── UserService.cs      # 用户服务
    └── WarehouseService.cs # 仓库服务
```

### 2.2 分层设计

```
┌─────────────────────────────────────────┐
│           表示层 (UI Layer)              │
│  - Forms/ 目录下的所有窗体类              │
│  - 负责用户界面展示和交互                 │
├─────────────────────────────────────────┤
│           业务逻辑层 (BLL)               │
│  - Services/ 目录下的服务类               │
│  - 处理业务规则和流程控制                 │
├─────────────────────────────────────────┤
│           数据访问层 (DAL)               │
│  - Helpers/DbHelper.cs                   │
│  - 负责数据库连接和SQL执行               │
├─────────────────────────────────────────┤
│           数据模型层 (Model)             │
│  - Models/ 目录下的实体类                 │
│  - 定义数据结构和枚举                     │
└─────────────────────────────────────────┘
```

## 3. 技术栈

### 3.1 开发框架

| 组件 | 版本 | 说明 |
|------|------|------|
| .NET | 10.0 | 目标框架 |
| Windows Forms | - | UI框架 |
| C# | 12.0 | 编程语言 |

### 3.2 数据库

| 组件 | 版本 | 说明 |
|------|------|------|
| Microsoft SQL Server | 2019+ | 数据库服务器 |
| Microsoft.Data.SqlClient | 6.1.4 | SQL Server 数据访问 |

### 3.3 第三方库

| 包名 | 版本 | 用途 |
|------|------|------|
| Microsoft.Data.SqlClient | 6.1.4 | SQL Server 数据库访问 |
| System.Configuration.ConfigurationManager | 9.0.0 | 配置文件管理 |
| System.Data.OleDb | 9.0.0 | Excel 导入导出支持 |

## 4. 数据库表结构

### 4.1 用户表 (DM_User)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| UserID | INT | PRIMARY KEY, IDENTITY | 用户ID |
| Username | NVARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| Password | NVARCHAR(256) | NOT NULL | 密码（SHA256哈希） |
| RealName | NVARCHAR(50) | NOT NULL | 真实姓名 |
| Permissions | NVARCHAR(500) | NULL | 权限列表（逗号分隔） |
| Workstation | NVARCHAR(50) | NULL | 工位 |
| IsActive | BIT | DEFAULT 1 | 是否启用 |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |
| LastLoginTime | DATETIME2 | NULL | 最后登录时间 |

### 4.2 刀模信息表 (DM_DieInfo)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| DieID | INT | PRIMARY KEY, IDENTITY | 刀模ID |
| DieCode | NVARCHAR(50) | NOT NULL, UNIQUE | 刀模编号 |
| CustomerName | NVARCHAR(100) | NOT NULL | 客户名称 |
| ProductName | NVARCHAR(100) | NOT NULL | 产品名称 |
| Structure | NVARCHAR(50) | NULL | 结构类型 |
| ModelType | NVARCHAR(50) | NULL | 模型类型 |
| LayoutType | NVARCHAR(50) | NULL | 排版方式 |
| FluteType | NVARCHAR(50) | NULL | 瓦楞类型 |
| Material | NVARCHAR(100) | NULL | 材料 |
| ManufactureLength | DECIMAL(10,2) | NULL | 制造长度 |
| ManufactureWidth | DECIMAL(10,2) | NULL | 制造宽度 |
| ManufactureHeight | DECIMAL(10,2) | NULL | 制造高度 |
| BlankLength | DECIMAL(10,2) | NULL | 下料长度 |
| BlankWidth | DECIMAL(10,2) | NULL | 下料宽度 |
| ProcessDesc | NVARCHAR(500) | NULL | 工艺描述 |
| RequiredProcesses | NVARCHAR(200) | NULL | 所需工序 |
| Status | INT | DEFAULT 0 | 状态（0待生产/1生产中/2已完成） |
| AuditStatus | INT | DEFAULT 0 | 审核状态（0未审核/1已审核） |
| SourceFactory | NVARCHAR(100) | NULL | 来源工厂 |
| ExternalOrderID | INT | NULL | 外部订单ID |
| DeliveryDate | DATE | NULL | 交货日期 |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |
| UpdateTime | DATETIME2 | NULL | 更新时间 |
| CreateUser | NVARCHAR(50) | NULL | 创建人 |
| Remark | NVARCHAR(500) | NULL | 备注 |

### 4.3 刀模工序表 (DM_DieProcess)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| ProcessID | INT | PRIMARY KEY, IDENTITY | 工序ID |
| DieID | INT | NOT NULL, FK | 刀模ID |
| ProcessName | NVARCHAR(50) | NOT NULL | 工序名称 |
| Status | INT | DEFAULT 0 | 状态（0待生产/1生产中/2已完成） |
| StartTime | DATETIME2 | NULL | 开始时间 |
| CompleteTime | DATETIME2 | NULL | 完成时间 |
| OperatorNo | NVARCHAR(50) | NULL | 操作员工号 |
| OperatorName | NVARCHAR(50) | NULL | 操作员姓名 |
| BoardLength | INT | NULL | 板长 |
| BoardWidth | INT | NULL | 板宽 |
| KnifeLength | INT | NULL | 刀长 |
| KnifeTraceLength | INT | NULL | 刀痕长度 |
| Formula | NVARCHAR(200) | NULL | 计算公式 |
| Amount | DECIMAL(18,2) | NULL | 金额 |
| PrevProcessID | INT | NULL | 前道工序ID |
| IsPrevCompleted | BIT | DEFAULT 0 | 前道工序是否完成 |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |

### 4.4 完工记录表 (DM_DieCompletion)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| CompletionID | INT | PRIMARY KEY, IDENTITY | 完工ID |
| DieID | INT | NOT NULL, FK | 刀模ID |
| CompleteTime | DATETIME2 | DEFAULT GETDATE() | 完工时间 |
| TotalAmount | DECIMAL(18,2) | NULL | 总金额 |
| OperatorNo | NVARCHAR(50) | NULL | 操作员工号 |
| OperatorName | NVARCHAR(50) | NULL | 操作员姓名 |
| Remark | NVARCHAR(500) | NULL | 备注 |

### 4.5 库位表 (DM_StorageLocation)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| LocationID | INT | PRIMARY KEY, IDENTITY | 库位ID |
| LocationCode | NVARCHAR(50) | NOT NULL, UNIQUE | 库位编号 |
| Area | NVARCHAR(50) | NOT NULL | 区域 |
| ShelfNo | NVARCHAR(20) | NOT NULL | 架号 |
| LayerNo | NVARCHAR(20) | NOT NULL | 层号 |
| PositionNo | NVARCHAR(20) | NOT NULL | 位号 |
| Description | NVARCHAR(200) | NULL | 描述 |
| Status | INT | DEFAULT 0 | 状态（0空闲/1占用/2禁用） |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |

### 4.6 库存表 (DM_DieInventory)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| InventoryID | INT | PRIMARY KEY, IDENTITY | 库存ID |
| DieID | INT | NOT NULL, FK, UNIQUE | 刀模ID |
| LocationID | INT | NULL, FK | 库位ID |
| StorageStatus | INT | DEFAULT 0 | 存储状态（0在库/1借出/2报废/3维修中） |
| InStockTime | DATETIME2 | NULL | 入库时间 |
| LastBorrowTime | DATETIME2 | NULL | 最后借出时间 |
| LastReturnTime | DATETIME2 | NULL | 最后归还时间 |
| TotalBorrowCount | INT | DEFAULT 0 | 总借用次数 |
| Remark | NVARCHAR(500) | NULL | 备注 |
| UpdateTime | DATETIME2 | DEFAULT GETDATE() | 更新时间 |

### 4.7 借用记录表 (DM_DieBorrowRecord)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| BorrowID | INT | PRIMARY KEY, IDENTITY | 借用ID |
| DieID | INT | NOT NULL, FK | 刀模ID |
| InventoryID | INT | NOT NULL | 库存ID |
| BorrowType | INT | NOT NULL | 借用类型（0生产领用/1外借/2调拨） |
| BorrowerNo | NVARCHAR(50) | NOT NULL | 借用人编号 |
| BorrowerName | NVARCHAR(50) | NOT NULL | 借用人姓名 |
| BorrowDept | NVARCHAR(100) | NULL | 借用部门 |
| BorrowTime | DATETIME2 | DEFAULT GETDATE() | 借用时间 |
| ExpectedReturnTime | DATETIME2 | NULL | 预计归还时间 |
| ActualReturnTime | DATETIME2 | NULL | 实际归还时间 |
| Purpose | NVARCHAR(500) | NULL | 用途 |
| Status | INT | DEFAULT 0 | 状态（0借用中/1已归还/2逾期） |
| ReturnOperatorNo | NVARCHAR(50) | NULL | 归还操作员工号 |
| ReturnOperatorName | NVARCHAR(50) | NULL | 归还操作员姓名 |
| Remark | NVARCHAR(500) | NULL | 备注 |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |

### 4.8 报废记录表 (DM_DieScrapRecord)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| ScrapID | INT | PRIMARY KEY, IDENTITY | 报废ID |
| DieID | INT | NOT NULL, FK | 刀模ID |
| InventoryID | INT | NOT NULL | 库存ID |
| ScrapReason | NVARCHAR(500) | NOT NULL | 报废原因 |
| ScrapType | NVARCHAR(50) | NULL | 报废类型 |
| ApplicantNo | NVARCHAR(50) | NOT NULL | 申请人工号 |
| ApplicantName | NVARCHAR(50) | NOT NULL | 申请人姓名 |
| ApplyTime | DATETIME2 | DEFAULT GETDATE() | 申请时间 |
| AuditorNo | NVARCHAR(50) | NULL | 审核人工号 |
| AuditorName | NVARCHAR(50) | NULL | 审核人姓名 |
| AuditTime | DATETIME2 | NULL | 审核时间 |
| AuditStatus | INT | DEFAULT 0 | 审核状态（0待审核/1已通过/2已驳回） |
| AuditRemark | NVARCHAR(500) | NULL | 审核备注 |
| ScrapTime | DATETIME2 | NULL | 报废时间 |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |

### 4.9 操作日志表 (DM_OperationLog)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| LogID | INT | PRIMARY KEY, IDENTITY | 日志ID |
| UserID | INT | NULL | 用户ID |
| Username | NVARCHAR(50) | NULL | 用户名 |
| OperationType | NVARCHAR(50) | NOT NULL | 操作类型 |
| OperationDesc | NVARCHAR(500) | NOT NULL | 操作描述 |
| DieID | INT | NULL | 刀模ID |
| IPAddress | NVARCHAR(50) | NULL | IP地址 |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |

### 4.10 系统配置表 (DM_SystemConfig)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| ConfigID | INT | PRIMARY KEY, IDENTITY | 配置ID |
| ConfigKey | NVARCHAR(100) | NOT NULL, UNIQUE | 配置键 |
| ConfigValue | NVARCHAR(500) | NULL | 配置值 |
| Description | NVARCHAR(200) | NULL | 描述 |
| CreateTime | DATETIME2 | DEFAULT GETDATE() | 创建时间 |
| UpdateTime | DATETIME2 | NULL | 更新时间 |

### 4.11 数据库版本表 (DM_DatabaseVersion)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| VersionID | INT | PRIMARY KEY, IDENTITY | 版本ID |
| VersionNumber | NVARCHAR(20) | NOT NULL | 版本号 |
| ScriptName | NVARCHAR(200) | NULL | 脚本名称 |
| AppliedDate | DATETIME2 | DEFAULT GETDATE() | 应用日期 |
| AppliedBy | NVARCHAR(50) | NULL | 应用人 |
| Description | NVARCHAR(500) | NULL | 描述 |

### 4.12 用户偏好设置表 (DM_UserPreference)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| PreferenceID | INT | PRIMARY KEY, IDENTITY | 偏好ID |
| UserID | INT | NOT NULL, FK, UNIQUE | 用户ID |
| Theme | NVARCHAR(20) | DEFAULT 'Light' | 主题 |
| DefaultPageSize | INT | DEFAULT 20 | 默认分页大小 |
| DateFormat | NVARCHAR(20) | DEFAULT 'yyyy-MM-dd' | 日期格式 |
| TimeFormat | NVARCHAR(20) | DEFAULT 'HH:mm:ss' | 时间格式 |
| DefaultPage | NVARCHAR(50) | DEFAULT 'DieList' | 默认页面 |
| UpdateTime | DATETIME2 | DEFAULT GETDATE() | 更新时间 |

### 4.13 表关系图

```
DM_User (1) ────────< (N) DM_DieInfo
                         │
                         ├──< (N) DM_DieProcess
                         ├──< (1) DM_DieCompletion
                         ├──< (1) DM_DieInventory ────> (N) DM_StorageLocation
                         ├──< (N) DM_DieBorrowRecord
                         ├──< (N) DM_DieScrapRecord
                         └──< (N) DM_OperationLog

DM_User (1) ────────< (1) DM_UserPreference
```

## 5. API接口文档

### 5.1 DieService - 刀模服务

#### 刀模基本信息操作

| 方法 | 签名 | 说明 |
|------|------|------|
| GetAllDies | `List<DieInfo> GetAllDies()` | 获取所有刀模列表 |
| GetDieById | `DieInfo? GetDieById(int dieId)` | 根据ID获取刀模信息 |
| SearchDies | `List<DieInfo> SearchDies(...)` | 搜索刀模（支持多条件） |
| CreateDie | `int CreateDie(DieInfo die, List<DieProcess> processes)` | 创建刀模 |
| UpdateDie | `bool UpdateDie(DieInfo die, List<DieProcess> processes)` | 更新刀模 |
| DeleteDie | `bool DeleteDie(int dieId)` | 删除刀模 |
| AuditDie | `bool AuditDie(int dieId, bool isApproved)` | 审核刀模 |
| IsDieCodeExists | `bool IsDieCodeExists(string dieCode, int? excludeDieId)` | 检查刀模编号是否存在 |

#### 刀模工序操作

| 方法 | 签名 | 说明 |
|------|------|------|
| GetDieProcesses | `List<DieProcess> GetDieProcesses(int dieId)` | 获取刀模的工序列表 |
| UpdateProcessStatus | `bool UpdateProcessStatus(int processId, ProcessStatus status, ...)` | 更新工序状态 |

### 5.2 ProductionService - 生产服务

#### 生产看板相关

| 方法 | 签名 | 说明 |
|------|------|------|
| GetProductionBoardData | `ProductionBoardData GetProductionBoardData(...)` | 获取生产看板数据 |

#### 完工查询相关

| 方法 | 签名 | 说明 |
|------|------|------|
| QueryCompletions | `List<CompletionRecord> QueryCompletions(...)` | 查询完工记录 |

#### 工序报产相关

| 方法 | 签名 | 说明 |
|------|------|------|
| GetDieProcessesForReport | `List<DieProcessForReport> GetDieProcessesForReport(int dieId)` | 获取工序列表（报产用） |
| GetAvailableDiesForReport | `List<DieInfoForReport> GetAvailableDiesForReport()` | 获取可报产刀模列表 |
| StartProcess | `bool StartProcess(int processId, string operatorNo, string operatorName)` | 开始工序生产 |
| CompleteProcess | `bool CompleteProcess(int processId, decimal? amount, ...)` | 完成工序生产 |
| IsPrevProcessCompleted | `bool IsPrevProcessCompleted(int processId)` | 检查前道工序是否完成 |

### 5.3 WarehouseService - 仓库服务

#### 库位管理

| 方法 | 签名 | 说明 |
|------|------|------|
| GetAllLocations | `List<StorageLocation> GetAllLocations()` | 获取所有库位 |
| GetLocationsByStatus | `List<StorageLocation> GetLocationsByStatus(LocationStatus status)` | 根据状态获取库位 |
| SearchLocations | `List<StorageLocation> SearchLocations(string keyword)` | 搜索库位 |
| GetLocationById | `StorageLocation? GetLocationById(int locationId)` | 根据ID获取库位 |
| CreateLocation | `int CreateLocation(StorageLocation location)` | 创建库位 |
| UpdateLocation | `bool UpdateLocation(StorageLocation location)` | 更新库位 |
| DeleteLocation | `bool DeleteLocation(int locationId)` | 删除库位 |
| IsLocationCodeExists | `bool IsLocationCodeExists(string locationCode, ...)` | 检查库位编号是否存在 |

#### 刀模库存管理

| 方法 | 签名 | 说明 |
|------|------|------|
| GetAllInventory | `List<DieInventory> GetAllInventory()` | 获取所有库存 |
| GetInventoryByStatus | `List<DieInventory> GetInventoryByStatus(StorageStatus status)` | 根据状态获取库存 |
| GetInStockInventory | `List<DieInventory> GetInStockInventory()` | 获取在库库存 |
| GetInventoryById | `DieInventory? GetInventoryById(int inventoryId)` | 根据ID获取库存 |
| GetInventoryByDieId | `DieInventory? GetInventoryByDieId(int dieId)` | 根据刀模ID获取库存 |
| UpdateInventoryLocation | `bool UpdateInventoryLocation(int inventoryId, int? locationId)` | 更新库存库位 |
| UpdateInventoryStatus | `bool UpdateInventoryStatus(int inventoryId, StorageStatus status)` | 更新库存状态 |

#### 借用记录管理

| 方法 | 签名 | 说明 |
|------|------|------|
| GetAllBorrowRecords | `List<DieBorrowRecord> GetAllBorrowRecords()` | 获取所有借用记录 |
| GetBorrowRecordsByStatus | `List<DieBorrowRecord> GetBorrowRecordsByStatus(BorrowStatus status)` | 根据状态获取借用记录 |
| GetBorrowRecordsByDieId | `List<DieBorrowRecord> GetBorrowRecordsByDieId(int dieId)` | 根据刀模ID获取借用记录 |
| SearchBorrowRecords | `List<DieBorrowRecord> SearchBorrowRecords(...)` | 搜索借用记录 |
| GetBorrowRecordById | `DieBorrowRecord? GetBorrowRecordById(int borrowId)` | 根据ID获取借用记录 |
| CreateBorrowRecord | `int CreateBorrowRecord(DieBorrowRecord record)` | 创建借用记录 |
| ReturnDie | `bool ReturnDie(int borrowId, string returnOperatorNo, ...)` | 归还刀模 |

#### 报废申请管理

| 方法 | 签名 | 说明 |
|------|------|------|
| GetAllScrapRecords | `List<DieScrapRecord> GetAllScrapRecords()` | 获取所有报废记录 |
| GetScrapRecordsByStatus | `List<DieScrapRecord> GetScrapRecordsByStatus(ScrapAuditStatus status)` | 根据状态获取报废记录 |
| GetScrapRecordById | `DieScrapRecord? GetScrapRecordById(int scrapId)` | 根据ID获取报废记录 |
| CreateScrapRecord | `int CreateScrapRecord(DieScrapRecord record)` | 创建报废记录 |
| AuditScrapRecord | `bool AuditScrapRecord(int scrapId, bool approved, ...)` | 审核报废记录 |
| DeleteScrapRecord | `bool DeleteScrapRecord(int scrapId)` | 删除报废记录 |

### 5.4 UserService - 用户服务

| 方法 | 签名 | 说明 |
|------|------|------|
| Login | `User? Login(string username, string password)` | 用户登录 |
| GetAllUsers | `List<User> GetAllUsers()` | 获取所有用户 |
| GetUserById | `User? GetUserById(int userId)` | 根据ID获取用户 |
| CreateUser | `int CreateUser(User user)` | 创建用户 |
| UpdateUser | `bool UpdateUser(User user)` | 更新用户信息 |
| UpdatePassword | `bool UpdatePassword(int userId, string newPassword)` | 更新用户密码 |
| DeleteUser | `bool DeleteUser(int userId)` | 删除用户 |
| IsUsernameExists | `bool IsUsernameExists(string username, int? excludeUserId)` | 检查用户名是否存在 |

### 5.5 ReportService - 报表服务

#### 完工统计

| 方法 | 签名 | 说明 |
|------|------|------|
| GetCompletionStatsByDie | `List<CompletionStatsByDie> GetCompletionStatsByDie(...)` | 按刀模统计 |
| GetCompletionStatsByCustomer | `List<CompletionStatsByCustomer> GetCompletionStatsByCustomer(...)` | 按客户统计 |
| GetCompletionStatsByDate | `List<CompletionStatsByDate> GetCompletionStatsByDate(...)` | 按日期统计 |

#### 工序统计

| 方法 | 签名 | 说明 |
|------|------|------|
| GetProcessStats | `List<ProcessStats> GetProcessStats(...)` | 获取工序统计数据 |
| GetProcessDetailStats | `List<ProcessDetailStats> GetProcessDetailStats(...)` | 获取工序明细数据 |

#### 库存统计

| 方法 | 签名 | 说明 |
|------|------|------|
| GetInventorySummaryStats | `InventorySummaryStats GetInventorySummaryStats()` | 获取库存汇总统计 |
| GetLocationDistributionStats | `List<LocationDistributionStats> GetLocationDistributionStats()` | 获取库位分布统计 |
| GetInventoryDetailStats | `List<InventoryDetailStats> GetInventoryDetailStats(...)` | 获取库存明细数据 |
| GetBorrowStats | `List<BorrowStats> GetBorrowStats(...)` | 获取借用记录统计 |

### 5.6 ConfigService - 配置服务

| 方法 | 签名 | 说明 |
|------|------|------|
| GetAllConfigs | `List<SystemConfig> GetAllConfigs()` | 获取所有配置项 |
| GetConfigValue | `string? GetConfigValue(string key, string? defaultValue)` | 获取配置值 |
| GetConfigValueInt | `int GetConfigValueInt(string key, int defaultValue)` | 获取整数配置值 |
| GetConfigValueBool | `bool GetConfigValueBool(string key, bool defaultValue)` | 获取布尔配置值 |
| GetSystemName | `string GetSystemName()` | 获取系统名称 |
| GetSystemVersion | `string GetSystemVersion()` | 获取系统版本 |
| GetDefaultPageSize | `int GetDefaultPageSize()` | 获取默认分页大小 |
| GetPasswordPolicy | `PasswordPolicy GetPasswordPolicy()` | 获取密码策略 |
| GetLockoutPolicy | `LockoutPolicy GetLockoutPolicy()` | 获取登录锁定策略 |
| UpdateConfig | `bool UpdateConfig(string key, string value)` | 更新配置值 |
| UpdateConfigs | `bool UpdateConfigs(Dictionary<string, string> configs)` | 批量更新配置 |
| InitializeDefaultConfigs | `bool InitializeDefaultConfigs()` | 初始化默认配置 |
| GetUserPreference | `UserPreference GetUserPreference(int userId)` | 获取用户个性化设置 |
| SaveUserPreference | `bool SaveUserPreference(UserPreference preference)` | 保存用户个性化设置 |
| ValidatePassword | `(bool isValid, string message) ValidatePassword(string password)` | 验证密码是否符合策略 |

## 6. 开发规范

### 6.1 命名规范

#### 类命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 窗体类 | Form 结尾 | `DieListForm`, `UserManageForm` |
| 服务类 | Service 结尾 | `DieService`, `UserService` |
| 模型类 | 名词 | `DieInfo`, `User`, `StorageLocation` |
| 辅助类 | Helper 结尾 | `DbHelper`, `ConfigHelper` |
| 枚举 | 名词 | `DieStatus`, `ProcessStatus` |
| 接口 | I 前缀 | `IDieService`（如有） |

#### 方法命名

| 操作 | 前缀 | 示例 |
|------|------|------|
| 获取单个 | Get | `GetDieById`, `GetUserById` |
| 获取列表 | GetAll / GetList | `GetAllDies`, `GetListByStatus` |
| 搜索 | Search | `SearchDies`, `SearchLocations` |
| 创建 | Create | `CreateDie`, `CreateUser` |
| 更新 | Update | `UpdateDie`, `UpdateUser` |
| 删除 | Delete | `DeleteDie`, `DeleteUser` |
| 检查 | Is / Check | `IsDieCodeExists`, `CheckPermission` |
| 验证 | Validate | `ValidatePassword` |

#### 变量命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 局部变量 | camelCase | `dieId`, `userName` |
| 私有字段 | _camelCase | `_connectionString`, `_statusLabel` |
| 常量 | UPPER_SNAKE_CASE | `DEFAULT_PAGE_SIZE` |
| 静态只读 | PascalCase | `ConnectionString` |

### 6.2 代码风格

#### 代码组织

```csharp
// 1. using 语句
using System;
using DieMaking.Models;

// 2. 命名空间
namespace DieMaking.Services;

// 3. 类/接口声明
public class DieService
{
    // 4. 常量
    private const int MAX_RETRY_COUNT = 3;
    
    // 5. 静态字段
    private static readonly object _lockObj = new();
    
    // 6. 实例字段
    private readonly ILogger _logger;
    
    // 7. 构造函数
    public DieService(ILogger logger)
    {
        _logger = logger;
    }
    
    // 8. 公共方法
    public List<DieInfo> GetAllDies() { }
    
    // 9. 私有方法
    private DieInfo MapToDieInfo(SqlDataReader reader) { }
}
```

#### 异常处理

```csharp
public DieInfo? GetDieById(int dieId)
{
    try
    {
        // 业务逻辑
    }
    catch (SqlException ex)
    {
        ExceptionHelper.HandleException(ex, $"获取刀模信息(ID:{dieId})");
        return null;
    }
    catch (Exception ex)
    {
        ExceptionHelper.HandleException(ex, $"获取刀模信息(ID:{dieId})");
        return null;
    }
}
```

#### 事务处理

```csharp
public int CreateDie(DieInfo die, List<DieProcess> processes)
{
    using var connection = DbHelper.CreateConnection();
    SqlTransaction? transaction = null;

    try
    {
        connection.Open();
        transaction = connection.BeginTransaction();
        
        // 执行业务操作
        
        transaction.Commit();
        return dieId;
    }
    catch (Exception ex)
    {
        transaction?.Rollback();
        ExceptionHelper.HandleException(ex, "创建刀模");
        return 0;
    }
}
```

### 6.3 注释规范

#### XML 文档注释

```csharp
/// <summary>
/// 根据ID获取刀模信息
/// </summary>
/// <param name="dieId">刀模ID</param>
/// <returns>刀模信息对象，未找到返回null</returns>
public DieInfo? GetDieById(int dieId)
{
    // 实现代码
}
```

#### 行内注释

```csharp
// 检查前道工序是否完成
if (!IsPrevProcessCompleted(processId))
{
    throw new BusinessException("前道工序尚未完成");
}
```

#### 复杂逻辑注释

```csharp
/*
 * 计算工序完成率：
 * 1. 统计该刀模所有工序数量
 * 2. 统计已完成工序数量
 * 3. 计算完成率 = 已完成 / 总数 * 100%
 */
var totalProcesses = processes.Count;
var completedProcesses = processes.Count(p => p.Status == ProcessStatus.Completed);
var completionRate = (double)completedProcesses / totalProcesses * 100;
```

### 6.4 数据库规范

#### 表命名

- 使用 `DM_` 前缀（DieMaking 缩写）
- 使用 PascalCase 命名
- 示例：`DM_DieInfo`, `DM_DieProcess`, `DM_User`

#### 字段命名

- 使用 PascalCase 命名
- 主键统一使用 `表名ID` 格式
- 外键使用 `关联表名ID` 格式
- 时间字段：`CreateTime`, `UpdateTime`, `CompleteTime`
- 状态字段：`Status`, `AuditStatus`, `StorageStatus`

#### 索引命名

- 主键索引：默认 `PK_表名`
- 唯一索引：`IX_表名_字段名`
- 普通索引：`IX_表名_字段名`
- 复合索引：`IX_表名_字段1_字段2`

### 6.5 安全规范

#### SQL 注入防护

```csharp
// 正确：使用参数化查询
var sql = "SELECT * FROM DM_DieInfo WHERE DieCode = @DieCode";
var result = DbHelper.ExecuteQuery(sql, mapper, new SqlParameter("@DieCode", dieCode));

// 错误：字符串拼接
var sql = $"SELECT * FROM DM_DieInfo WHERE DieCode = '{dieCode}'"; // 危险！
```

#### 密码存储

```csharp
// 使用 SHA256 哈希存储密码
private static string HashPassword(string password)
{
    using var sha256 = SHA256.Create();
    var bytes = Encoding.UTF8.GetBytes(password);
    var hash = sha256.ComputeHash(bytes);
    return Convert.ToBase64String(hash);
}
```

#### 权限检查

```csharp
public void DeleteDie(int dieId)
{
    if (!CurrentUser.HasPermission(PermissionKeys.DieEdit))
    {
        throw new UnauthorizedAccessException("没有删除刀模的权限");
    }
    // 删除逻辑
}
```

## 7. 枚举定义

### 7.1 刀模状态 (DieStatus)

```csharp
public enum DieStatus
{
    Pending = 0,      // 待生产
    InProgress = 1,   // 生产中
    Completed = 2,    // 已完成
    OnHold = 3,       // 暂不生产
    NotRequired = 4   // 无需生产
}
```

### 7.2 审核状态 (AuditStatus)

```csharp
public enum AuditStatus
{
    Unaudited = 0,    // 未审核
    Audited = 1       // 已审核
}
```

### 7.3 工序状态 (ProcessStatus)

```csharp
public enum ProcessStatus
{
    Pending = 0,      // 待生产
    InProgress = 1,   // 生产中
    Completed = 2     // 已完成
}
```

### 7.4 库位状态 (LocationStatus)

```csharp
public enum LocationStatus
{
    Free = 0,      // 空闲
    Occupied = 1,  // 占用
    Disabled = 2   // 禁用
}
```

### 7.5 存储状态 (StorageStatus)

```csharp
public enum StorageStatus
{
    InStock = 0,   // 在库
    Borrowed = 1,  // 借出
    Scrapped = 2,  // 报废
    Repairing = 3  // 维修中
}
```

### 7.6 借用类型 (BorrowType)

```csharp
public enum BorrowType
{
    Production = 0,   // 生产领用
    External = 1,     // 外借
    Transfer = 2      // 调拨
}
```

### 7.7 借用状态 (BorrowStatus)

```csharp
public enum BorrowStatus
{
    Borrowing = 0,    // 借用中
    Returned = 1,     // 已归还
    Overdue = 2       // 逾期
}
```

### 7.8 报废审核状态 (ScrapAuditStatus)

```csharp
public enum ScrapAuditStatus
{
    Pending = 0,      // 待审核
    Approved = 1,     // 已通过
    Rejected = 2      // 已驳回
}
```

## 8. 配置文件键名

### 8.1 基本设置

| 键名 | 默认值 | 说明 |
|------|--------|------|
| SystemName | 刀模管理系统 | 系统名称 |
| CompanyName | - | 公司名称 |
| SystemVersion | 1.0.0 | 系统版本 |
| DefaultPageSize | 20 | 默认分页大小 |
| DateFormat | yyyy-MM-dd | 日期格式 |
| TimeFormat | HH:mm:ss | 时间格式 |
| DateTimeFormat | yyyy-MM-dd HH:mm:ss | 日期时间格式 |
| FileUploadPath | C:\DieMaking\Uploads | 文件上传路径 |

### 8.2 安全设置

| 键名 | 默认值 | 说明 |
|------|--------|------|
| PasswordMinLength | 6 | 密码最小长度 |
| PasswordRequireUppercase | false | 密码要求大写字母 |
| PasswordRequireLowercase | false | 密码要求小写字母 |
| PasswordRequireDigit | false | 密码要求数字 |
| PasswordRequireSpecialChar | false | 密码要求特殊字符 |
| MaxLoginFailures | 5 | 最大登录失败次数 |
| LockoutDuration | 30 | 账户锁定时间(分钟) |
| SessionTimeout | 30 | 会话超时时间(分钟) |

### 8.3 日志设置

| 键名 | 默认值 | 说明 |
|------|--------|------|
| LogLevel | Info | 日志级别 |
| LogRetentionDays | 30 | 日志保留天数 |

### 8.4 备份设置

| 键名 | 默认值 | 说明 |
|------|--------|------|
| AutoBackup | false | 自动备份 |
| BackupPath | C:\DieMaking\Backup | 备份路径 |
| BackupRetentionDays | 30 | 备份保留天数 |
| BackupTime | 02:00 | 备份时间 |

### 8.5 功能开关

| 键名 | 默认值 | 说明 |
|------|--------|------|
| EnableAudit | true | 启用审核流程 |
| EnableOperationLog | true | 启用操作日志 |
