# 移除"保存草稿"按钮 - 实施计划

## 方案说明
根据用户选择，采用方案C：直接移除"保存草稿"按钮，简化界面。

## 实施步骤

### 步骤1：修改 DieAddForm.cs
- 移除 `btnSaveDraft` 按钮的声明
- 移除 `btnSaveDraft` 的初始化和事件绑定代码
- 调整其他按钮的位置布局（可选）

### 步骤2：清理相关代码
- 移除 `BtnSaveDraft_Click` 事件处理方法
- 简化 `SaveDie` 方法，移除 `isDraft` 参数（因为不再需要区分）

### 步骤3：验证修改
- 编译项目确保无错误
- 运行测试确保新增/编辑刀模功能正常

## 修改文件清单
1. `Forms/Die/DieAddForm.cs` - 主要修改文件

## 预期结果
- 新增刀模和编辑刀模界面只保留"保存"和"取消"按钮
- 功能与之前保持一致（因为保存草稿原本就没有特殊逻辑）
